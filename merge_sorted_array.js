let arr = [2, 5, 6];
let arr2 = [1, 3, 4, 8];
let add = new Array(arr.length + arr2.length);

function merge(arr, arr2) {
  let k = 0;
  let i = 0;
  for (s = 0; i < add.length; s++) {
    if (a[i] <= a[j]) {
      add[s] = a[i];
      i++;
    } else {
      add[s] = a[j];
      j++;
    }
  }
}
